{
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "bench",
      "command": "export PATH=\"/usr/local/bin:$HOME/.local/bin:$(printf '%s' \"$PATH\" | tr ':' '\\n' | awk 'NF && $0 !~ /nvm/' | paste -sd: -)\" && bash .cursor/start.sh && cd \"$HOME/frappe-bench\" && bench start"
    }
  ]
}
